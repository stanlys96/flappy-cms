'use strict';

/**
 * presale-wallet-address service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::presale-wallet-address.presale-wallet-address');
