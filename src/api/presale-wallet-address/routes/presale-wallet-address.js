'use strict';

/**
 * presale-wallet-address router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::presale-wallet-address.presale-wallet-address');
