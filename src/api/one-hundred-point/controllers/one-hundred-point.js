"use strict";

/**
 * one-hundred-point controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController(
  "api::one-hundred-point.one-hundred-point"
);
